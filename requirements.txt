# torch 与 torchvision 请安装相互匹配的版本。
# 已安装可用 GPU 版本（例如 Kaggle）时，无需重新安装 PyTorch。
torch>=2.2
torchvision>=0.17
segmentation-models-pytorch>=0.5,<0.6
numpy>=1.24
Pillow>=9.1
PyYAML>=6.0
tqdm>=4.66
matplotlib>=3.7
